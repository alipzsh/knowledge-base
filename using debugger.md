- try different parts of the code in the console.
  e.g. to see if a condition passes
- see under what condition the protections could be bypassed
  e.g. sending a particular string that is expected that hides a payload
  somewhere
