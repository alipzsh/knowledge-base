# OpenBSD

`dmesg | grep "not configured"` to see the stuff not supported in the kernel.

seems more interesting than FreeBSD.
Doesn't support wifi on ASUS.

also it's unlikely that anytime soon I'd want to move all the way to BSD. and I want something
on the hardware, so FreeBSD it is because wifi works on ASUS.
