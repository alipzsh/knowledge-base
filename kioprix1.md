# kioprix1
* SMB:The Server Message Block protocol (SMB protocol) is client-server
  communication protocol used for sharing access to files, printers,
  serial ports and other resources on a network. It can also carry
  transaction.

  * you can use `smbclient -L` to find the version.
