- install driver pack solutions

# GPU and CPU

- if the exist in sherkat/displayDrivers? install : download and save them to
sherkat/displayDrivers

- install/upgrade the ones that exist -> be sure to uncheck the other sections

# manual install/update

-> check manage -> device sth
-> details -> hardware id (or sth)
-> search online -> e.g. choose driverpack zip
-> extract -> add the path to update section

- if somethings can't be installed directly (perhaps because win11 is not supported)
  add, update the path manually.
