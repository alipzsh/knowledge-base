# action object

is an instance of a class that handles user requests. EX: an action object for
[LoginAction](106/LoginAction.java) class.

could return results strings mapped to view pages in [struts.xml](106/struts.xml).
