CSP (Content Security Policy) rules are HTTP headers or meta tags that control which resources (like scripts, styles, images) a browser is allowed to load for a given web page.

```http
Content-Security-Policy: script-src 'self' https://trusted.cdn.com
```