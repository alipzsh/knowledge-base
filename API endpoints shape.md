 general structure or pattern of the URLs (endpoints) in an API — like templates or blueprints for how endpoints are formed.
 
### Common REST API Endpoint Shapes

- `GET /resources` — list all resources
    
- `GET /resources/:id` — get one resource
    
- `POST /resources` — create a new resource
    
- `PUT /resources/:id` — update a full resource
    
- `PATCH /resources/:id` — update part of a resource
    
- `DELETE /resources/:id` — delete a resource