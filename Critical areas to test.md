First work on these:

- authentication class
  - OAuth (all providers)
  - linking accounts
- switching between applications (desktop, mobile, web, sending you between them.)
- uploader sections
- links or HTML inputs
- application specific sections
- sensitive APIs
- deep links
