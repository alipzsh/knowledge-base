# maven directory layout

`src`: all the source material for building the project.
  * `main`: main build
    * `java`: contains package hierarchy
    * `resources`: a structure to be copied to the target (build) classpath
  * `test`: test code
    * `java`
    * `resources`
